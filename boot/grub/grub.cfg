# ==================================================
#   VecOS 1.0 - Configuração do Boot (GRUB)
#   Base: Ubuntu / KDE Plasma
#   Interface: VecUI
# ==================================================

set default=0
set timeout=5
set gfxmode=auto
set gfxpayload=keep
set theme=/boot/grub/themes/vecos/theme.txt
set menu_color_normal=white/black
set menu_color_highlight=black/white

# Logo e título
insmod gfxterm
terminal_output gfxterm
echo "🔥 Iniciando VecOS 1.0 - Sistema Fluido e Seguro 🔥"

# ==================================================
#   Entradas de Boot
# ==================================================

menuentry "Iniciar VecOS 1.0 (Live Mode)" {
    set gfxpayload=keep
    linux   /boot/vmlinuz boot=live quiet splash --
    initrd  /boot/initrd.img
}

menuentry "Instalar VecOS 1.0 no Disco" {
    set gfxpayload=keep
    linux   /boot/vmlinuz boot=install quiet splash --
    initrd  /boot/initrd.img
}

menuentry "Recuperar Sistema (Modo Avançado)" {
    set gfxpayload=keep
    linux   /boot/vmlinuz boot=recovery nomodeset --
    initrd  /boot/initrd.img
}

# ==================================================
#   Opções Extras
# ==================================================

submenu "Opções Avançadas para VecOS" {
    menuentry "Iniciar VecOS (Modo Texto)" {
        linux   /boot/vmlinuz 3
        initrd  /boot/initrd.img
    }

    menuentry "Iniciar VecOS sem Modo Gráfico" {
        linux   /boot/vmlinuz nomodeset
        initrd  /boot/initrd.img
    }
}
